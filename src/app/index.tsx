import { MainLayout } from '@/components/layouts/main-layout';
import { Navbar } from '@/features/navbar/navbar';

export const App = () => {
    return (
        <MainLayout>
            <Navbar />
        </MainLayout>
    );
};
